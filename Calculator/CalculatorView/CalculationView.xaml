<UserControl x:Class="CalculatorView.CalculationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:CalculatorView"
             xmlns:vm="clr-namespace:CalculatorViewModel;assembly=CalculatorViewModel"
             d:DataContext="{d:DesignInstance {x:Type vm:CalculationViewModel }}"
             mc:Ignorable="d"
             d:DesignWidth="1200"
             d:DesignHeight="1000">

    <Grid Background="Gray">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80*" />
            <ColumnDefinition Width="20*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="10*" />
            <RowDefinition Height="10*" />
            <RowDefinition Height="80*" />
        </Grid.RowDefinitions>

        <local:HeaderView Grid.Row="0" DataContext="{Binding HeaderViewModel}" />

        <local:DisplayView Grid.Row="1" DataContext="{Binding DisplayViewModel}"/>


        <local:MathFunctionsView Grid.Row="2" DataContext="{Binding MathFunctionsViewModel}">
        </local:MathFunctionsView>
    </Grid>
</UserControl>